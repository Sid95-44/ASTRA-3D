/**
 * nasaAPI.js
 * ------------------------------------------------------------------------
 * NOT USED YET. Reserved for Milestone 4+ (Space Objects) and Milestone 6
 * (Earth Observation), where ASTRA 3D will fetch live data from NASA's
 * open APIs (e.g. api.nasa.gov: NEO Web Service, EONET, EPIC imagery).
 *
 * Kept as an empty module now so the project structure matches the target
 * architecture and future milestones only need to fill this in rather than
 * restructure the app. Importing this file currently has no side effects.
 * ------------------------------------------------------------------------
 */

export const NASA_API_BASE = "https://api.nasa.gov";

// Implemented in a later milestone.
export async function fetchNearEarthObjects() {
  throw new Error("nasaAPI.fetchNearEarthObjects() is not implemented until Milestone 4.");
}